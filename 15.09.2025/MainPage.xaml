<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp2.MainPage">

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*"/>
            <RowDefinition Height="0.8*"/>
            <RowDefinition Height="0.1*"/>
            <RowDefinition Height="0,1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Entry x:Name="Name" Placeholder="product" Grid.Row="0" Grid.ColumnSpan="2" Grid.Column="0" MaximumHeightRequest="60"/>
        <Button x:Name="Add_Product" Text="Add Product" Grid.Row="0" Grid.Column="2" MaximumHeightRequest="50"/>
        <Entry x:Name="Quantity" Placeholder="quantity" Grid.Row="1" Grid.ColumnSpan="2" Grid.Column="0" MaximumHeightRequest="60"/>
        <CollectionView ItemsSource="{Binding Products}" Grid.Row="2">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Label Text="{Binding Products}" />
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Button Text="Delete" Grid.Row="3" Grid.Column="1" MaximumHeightRequest="60" />


    </Grid>

</ContentPage>
